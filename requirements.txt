# --- Ingestion (batch loader into Snowflake RAW) ---
snowflake-connector-python>=3.7.0
python-dotenv>=1.0.0
requests>=2.31.0

# --- Transformation (dbt on Snowflake) ---
dbt-core>=1.7.0
dbt-snowflake>=1.7.0

# --- Orchestration (Airflow) ---
# Heavy + Linux-oriented; on Windows run Airflow via WSL or Docker.
apache-airflow>=2.8.0

# --- Bonus: Spark historical processing ---
pyspark>=3.5.0
