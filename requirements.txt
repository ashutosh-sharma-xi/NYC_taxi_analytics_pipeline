# Core pipeline — ingestion into Snowflake RAW + dbt transforms.
# This is all `scripts/pipeline.sh` needs.
#
# Optional extras live in separate files (install only if you use them):
#   requirements-airflow.txt   to run the Airflow DAG (dags/)
#   requirements-spark.txt     for the PySpark bonus (spark/)
#   eda/requirements.txt       for the local EDA notebook (DuckDB)
snowflake-connector-python>=3.7.0
python-dotenv>=1.0.0
requests>=2.31.0
dbt-core>=1.7.0
dbt-snowflake>=1.7.0
